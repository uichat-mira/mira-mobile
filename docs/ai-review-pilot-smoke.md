# Mira AI Review Pilot Probe

This file exists only to exercise the Mira Organization AI Review path on a disposable `feat/* -> dev` pull request.

It must not be merged into `dev`. The pilot checks trusted base/profile/task loading, provider execution, normalized publication, exact head identity, and refresh behavior after a second push.

Probe revision: 2.
