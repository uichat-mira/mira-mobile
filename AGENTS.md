# AGENTS.md

本文件是 `uichat-mira/mira-mobile` 仓库中编码智能体的根级工作约束。

任何智能体在创建、修改或删除代码前，都必须先阅读本文件。若子目录中以后出现更具体的 `AGENTS.md`，子目录规则可以补充本文件，但不得绕过这里定义的产品边界、安全要求、分支流程和验收标准。

## 1. 项目定位

`uichat-mira/mira-mobile` 是 Mira 的官方移动端工程。

移动端不是桌面端的缩小复制品。当前产品已经形成两条明确链路：

- **Remote Host 路径**：通过配对 / Relay 连接 Mira Host，消费 Host 侧会话、Agent、Tool 等能力。
- **Local Provider 路径**：用户在设备侧配置自己的模型 Provider / API Key，由 Mobile 直接进行本地会话与模型调用。

移动端负责移动交互、设备能力、可靠连接，以及已经在仓库合同中明确的本地运行时能力；不得因为“移动端”三个字就复制桌面端全部系统，也不得绕过 Host / Harness 的权威边界。

当前仓库仍处于快速演进阶段。首要目标是稳定、可维护、可验证，而不是快速堆叠大量页面和功能。

## 2. 职责边界

### 移动端负责

- 应用启动、路由、页面壳和生命周期管理。
- 网络层、连接状态、超时、重试、断线恢复与错误呈现。
- Mira Host 的发现、地址配置、配对、认证和取消授权。
- Remote Host 会话与 Agent 状态的移动端呈现和交互。
- Local Provider 配置、设备侧凭据安全存储、本地会话与已定义的模型调用路径。
- 已有 Mobile Agent Loop 的前台编排、运行状态呈现和移动端审批交互。
- 必要的本地缓存、离线状态和设备安全存储。
- 相机、相册、文件选择、通知、分享等移动设备能力。
- Android 与 iOS 的平台适配，以及移动端构建、测试和发布基建。

### Mira Host / 主工程负责

- Remote Host 路径中的 Provider、模型与服务端凭据管理。
- Host 侧持久 Agent Runtime、Planner、Harness、Tool、Skill、MCP 与实际工具执行。
- 知识库、RAG、服务端会话状态与业务事实来源。
- Remote Host 的统一消息协议、Agent 状态、Artifact 和工具执行结果。
- Host / Harness 权限判定及其他不应由 Mobile 自行伪造的业务事实。

### 当前关键边界

- Local Provider API Key 留在 Mobile；Host Provider / Tool / MCP 凭据不得下发到 Mobile。
- Mobile 可以编排已明确的本地 Agent Loop，但当前 Tool Gateway 仍以远程执行为主，实际工具执行权威属于 Host / Harness。
- 不在客户端伪造工具执行、审批结果或服务端状态。
- 不凭想象定义并直接绑定尚未确认的 Mira API。
- 不把认证令牌、模型密钥或敏感配置写入源码。

产品或协议边界发生变化时，以实际代码 / 协议、对应 Issue 和仓库文档的最新合同为准，不沿用本文件中的过时描述。

## 3. 当前交付原则

优先保证两条链路各自可验证：

1. Remote Host 能可靠配对、连接、恢复并正确呈现远端事实。
2. Local Provider 能安全保存凭据、创建本地会话并稳定完成模型调用。
3. Agent / Tool 等能力必须明确执行权威和凭据边界，不能因 UI 接入而隐式改变合同。
4. 网络切换、应用进入后台或短暂断线后的行为必须可解释、可恢复。
5. 失败时给出明确、可操作的错误信息。
6. Android / iOS 差异必须显式验证，不能只按单平台推断。

不为了“移动端功能齐全”而复制桌面端设置面板、服务端状态机或未确认的 MCP / Tool 执行系统。

## 4. 协议优先

移动端与 Mira Host 的协议，以及 Local Provider 的线协议，是跨边界代码最重要的契约。

- 先查找仓库内已有协议、类型、Issue、任务卡或架构说明，再写网络代码。
- 不得根据 UI 需求猜测 endpoint、字段、事件名或错误码。
- 协议尚未确定时，先建立可替换的 Adapter 接口并明确标注待确认项。
- 页面组件不得散落调用 HTTP、WebSocket 或 SSE；统一通过客户端 / Runtime 层访问。
- 外部协议数据进入应用后必须经过解析和校验。
- Mock 数据必须集中管理并明确切换，不得混入真实连接路径。
- 协议发生破坏性变更时，必须说明兼容范围和迁移方式。
- Provider 特例应通过明确的配置 / adapter 机制表达，不允许不断堆叠按 URL 猜厂商的条件分支。

## 5. 技术栈决策

仓库当前技术栈已经落定为 React Native + TypeScript，并包含必要的 Kotlin / iOS Native bridge。编码智能体不得在单个任务中擅自替换主技术栈、路由、状态管理、构建系统或发布体系。

引入新的关键依赖、原生模块、持久化方案或 Provider / Agent 基础设施前，应先确认：

- 当前仓库是否已有可复用能力；
- 是否会改变 Android / iOS 支持范围；
- 是否影响构建、签名、更新或发布渠道；
- 是否改变 Remote Host / Local Provider / Tool Gateway 的既有边界；
- 是否存在更小的实现方式。

技术选型优先考虑长期维护、依赖克制、国内网络环境下可稳定安装构建、便于测试，以及可随 Mira 协议演进。TypeScript 必须保持严格类型检查，不以大量 `any`、非空断言或强制类型转换绕过问题。

## 6. 安全要求

- Token、配对凭据、Provider API Key 和刷新凭据必须使用适合其信任边界的平台安全存储。
- Remote Host 凭据与 Local Provider 凭据不得混用或透明搬运。
- 日志、错误上报、截图和调试面板不得泄露凭据或完整敏感消息。
- 生产连接默认使用安全传输；局域网明文连接只能作为明确标识的开发模式。
- 不自动信任同一局域网内发现的任意设备。
- 权限申请按需触发并解释用途。
- 不提交 `.env`、签名文件、证书、私钥、真实账号、构建产物或本机路径。

## 7. 编码原则

- 只解决当前明确问题，不做无关重构。
- 优先小步提交，保证每一步都能构建或独立审查。
- 避免过早抽象，只有真实重复或明确扩展点才提取通用层。
- UI 保持简洁、克制、可触达，不引入笨重的后台管理式设计。
- 业务状态、网络状态和临时 UI 状态分开管理。
- 错误不得只 `console.log` 后吞掉。
- 不静默修改锁文件、CI、构建系统或原生工程配置。
- 引入依赖前说明用途，优先选择维护活跃、体积合理、许可证兼容的方案。
- 删除或替换已有实现前，先确认其兼容、平台或测试职责。

代码标识、协议字段和提交信息使用英文；协作文档可以使用中文。注释解释“为什么”，不要重复代码本身。

## 8. 智能体工作流程

每次任务按以下顺序执行：

1. 阅读 `AGENTS.md`、`README.md`、对应 Issue / 任务卡、相关文档和目标文件。
2. 检查当前分支、工作区状态和现有实现，避免覆盖他人改动。
3. 以代码 / 配置 / Runtime 为当前技术事实，以 Issue 为该工作项事实；发现文档冲突时先指出冲突。
4. 说明目标、影响范围、已知约束和仍需确认的假设。
5. 先完成最小可验证改动，再考虑扩展。
6. 执行与改动风险匹配的仓库验证；代码改动默认至少覆盖类型检查、Lint 和相关测试。
7. 检查 diff，移除调试代码、无关格式变化和意外生成文件。
8. 汇报修改内容、验证结果、风险和未完成事项。

命令失败时不得伪称通过。必须区分代码问题、本机环境问题、缺少账号或证书，以及尚未确定的协议 / 外部依赖问题。

## 9. 禁止的智能体行为

- 未读现有代码就整仓重写。
- 为展示“完成度”生成大量不可运行的占位页面。
- 虚构 API、测试结果、设备验证结果或构建成功状态。
- 将所有逻辑塞进单个页面、Store 或网络文件。
- 为简单需求引入庞大框架或不必要的基础设施。
- 通过禁用类型检查、Lint、测试或安全校验让构建表面通过。
- 擅自改变产品定位、仓库权限、环境模型、发布流程或跨仓库契约。
- 未经维护者明确要求，直接修改 Mira Desktop、Host 或服务端仓库。

## 10. 分支与发布流程

仓库遵循 Mira Organization 的标准环境模型：

```text
feat/* -> dev -> test -> prod
```

该模型已经启用。不得再按“尚未初始化的目标流程”处理当前仓库。

### `feat/*`

- 新的功能、修复、文档和基础设施改动正常情况下都从最新 `dev` 创建 `feat/<topic>` 工作分支。
- `feat/*` 只做 CI，不部署共享环境。
- 工作分支正常只向 `dev` 提交 Pull Request。
- 不直接把工作分支 PR 指向 `test` 或 `prod`。
- 仓库中保留的历史 `feature/*`、`fix/*`、`docs/*`、`chore/*` 分支是迁移前历史，不代表新工作继续沿用旧命名。

### `dev`

- 日常集成与开发验证分支，接收经过自检的工作分支。
- 运行完整 Mobile CI，并维护 dev Release / R2 产物。
- 可以包含尚待联调的功能，但必须保持可构建，并明确记录已知问题。
- 不在 `dev` 上长期直接施工；新的修改仍应创建工作分支。
- 进入下一阶段时，由 `dev` 向 `test` 晋级。

### `test`

- 测试和候选发布分支，只接收来自 `dev` 的晋级改动。
- 用于集成测试、设备验证、回归测试和 Mira Host / Provider 联调。
- 测试发现问题后，应从最新 `dev` 创建新的 `feat/*` 修复分支，再重新走 `dev -> test`。
- 不允许仅在 `test` 上形成无法回流到 `dev` 的孤立修复。

### `prod`

- 生产来源分支，只接收来自 `test` 的晋级改动。
- 不直接开发、不跳过测试环境合并功能。
- 合并前必须确认测试结果、已知风险、协议兼容性和必要的发布说明。
- 未经维护者明确宣布紧急流程，不允许直接在 `prod` 制作 hotfix。

### `predev`

`predev` 是本仓库历史形成的缓冲 / 预验证分支，保留现有专用工作流职责，但不属于 Organization 标准 `feat/* -> dev -> test -> prod` 环境链。不得让 `predev` 绕过 `prod` 承担生产发布。

### 同步原则

- 文档、协议、CI 和配置变更与代码使用同一晋级流程。
- 一次晋级应尽量传递相同的已验证改动，不在多个环境分支分别手工复制内容。
- `AGENTS.md` 等根级协作约束应随正常晋级保持一致。
- 合并后应回读目标分支，确认文件内容和提交确实生效。
- GitHub 默认分支只是 UI / 工作流选择，不重新定义环境职责。

### 提交与 PR

- 一个提交只承担一个清晰目的。
- 提交信息应说明实际变化，例如：
  - `feat: add host pairing flow`
  - `fix: resume stream after reconnect`
  - `docs: align mobile branch workflow`
- PR 必须包含目标、主要改动、验证方式、已知风险和协议影响；涉及 UI 时按需要补截图或录屏。
- 晋级 PR 必须明确来源和目标，例如 `dev -> test`、`test -> prod`。

## 11. 完成定义

任务只有满足以下条件才可称为完成：

- 实现符合当前 Issue / 任务卡和本文件的职责边界。
- 与风险匹配的类型检查、测试、构建或文档审计已经执行并如实记录结果。
- 关键失败路径得到处理。
- Android / iOS 影响已说明；无法实机验证时明确写出。
- 没有提交秘密、本地配置、调试残留或无关生成文件。
- 协议、行为或工程结构变化时，相应文档已经更新。
- 修改已按 `feat/* -> dev -> test -> prod` 到达任务要求的目标环境；不要求每个工作项都发布到生产。
- 维护者可以根据说明复现验证过程。

## 12. 默认判断

需求不明确时，默认选择：

- 真实代码 / Runtime 优先于过时文档。
- Issue 的明确范围优先于聊天中已经失效的施工记忆。
- 稳定连接优先于页面数量。
- 协议清晰优先于快速硬编码。
- 可验证优先于“看起来完成”。
- 小而清晰优先于复杂而通用。
- 明确双链路边界优先于让 Local / Remote 能力互相渗漏。

遇到可能影响 Mira 主工程协议、认证模型、Provider / Agent / Tool 权威边界或产品定位的决定，停止扩张实现，记录具体问题并交由维护者确认。
